import { GameManager } from "~/app/_components/game-manager";

export default function GamesPage() {
  return (
    <main className="flex min-h-screen flex-col items-center pt-8">
      <GameManager />
    </main>
  );
} 